__pycache__/
*.bak
*.bak.*
.DS_Store
*.log

# Runtime PRIL start-state markers (orchestrator-written / hook-read; not source).
docs/context/.active-feature
docs/context/.start-gate

# Real secrets: only .env.example is committed (REQ-B-003 / NGOAL-B-001).
.env

# Local update-mechanism snapshots + session tooling (per-user runtime, never source)
.plumbline/
.claude/homunculus/
.devin/

# plumbline: agent runtime state (volatile, never source) — PLUM-11
.claude-flow/
.swarm/
.hive-mind/
# plumbline: end agent runtime state
